---
-name:install nginx and start services
  hosts:all
  become:true

 tasks:
  -name:install nginx
      yum:
      name:mariadb
      state:latest
   
   -name: start mariadb
     service:
	   name: started
	   
	   
	   
	   
	   
	   ---
-name:install mariadb and start services
  hosts:all
  become:true

 tasks
  -name:install mariadb
    yum
      name:mariadb
      state:latest
   
   -name:start mariadb
     service
	   -name:started